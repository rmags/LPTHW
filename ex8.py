# Assigning a variable to be a string compromised of 4 potential modifiable # sub-strings that can be defined by the user, each separated by a spaceformatter = " %r %r %r %r"# printing 6 different statements using formatting variable's print formatter % (1,2,3,4)print formatter % ("one", "two", "three", "four")print formatter % (True, False, False, True)print formatter % (formatter, formatter, formatter, formatter)print formatter % (	"I had this thing.", 	"that you could type up right.",	"But it didn't sing." ,	"So I said goodnight.")